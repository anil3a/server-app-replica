services:
  server-app-replica:
    build:
      context: .
      dockerfile: builds/Dockerfile.build
    container_name: server-app-replica
    image: server-app-replica
    volumes:
      - ./html:/var/www/html
